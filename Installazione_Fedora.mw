{{InCorso|autore=GabrieleTrombini}}

Questa guida si propone di trattare l'installazione della release '''''Fedora 16 "Verne"''''', su un personal computer con architettura a 32 bit e utilizzando tre tipi di media a disposizione:
* DVD;
* CD per l'istallazione da rete;
* CD live desktop; 
Tutte queste versioni sono liberamente scaricabili e messe a disposizione dal [http://fedoraproject.org/it/get-fedora-all FedoraProject].<br />
__TOC__
{{Scrittura|In questa guida, per illustrare il processo di installazione, è stata utilizzata la macchina virtuale qemu/kvm che utilizza la nomenclatura dei dischi '''''vdx''''' anzichè '''''sdx''''' dei sistemi non virtualizzati.<br />

Inoltre la capacità dei dischi riprodotti nelle immagini è puramente indicativo, gli hard disk attuali permettono un partizionamento di certo soddisfacente per qualsiasi utilizzo.}} 

{{Warning|Tutti e tre i metodi di installazione richiedo l'avvio da DVD/CD rom con il disco inserito.<br />
L'immagine ISO che è stata scaricata dal link del FedoraProject sopra riportato '''deve essere masterizzata come immagine e non altro'''.}}

{{Warning|E' consigliabile creare un dischetto a valere sulla partizione di ripristino, qualora fosse presente, del sistema operativo Windows a scanso di eventuali problemi futuri e, comunque, di eseguire un backup dei propri dati importanti.}}

==Installazione da DVD==

Come da avviso occorre inserire il disco ed eseguire il boot da DVD.<br />
All'avvio si presenta subito una delle novità di questa versione di Fedora: una scelta di opzioni differenti dalle "storiche".<br /> 
A parte il countdown, che stavolta non viene interrotto premendo un tasto, ripartendo invece da 60 secondi, (può essere fermato premendo le frecce, su e giù, che permettono altresì di scorrere tra le selezioni), risulta evidente la voce ''Troubleshooting'', per la quale, selezionandola e premendo invio, vengono proposte queste azioni:
[[File:Install_f16_1.png|thumb|250px|Schermata di avvio]]
* ''Install Fedora in basic graphics mode'', per chi avesse problemi hardware, in particolare sulla scheda video (al di sotto compare la descrizione breve), userà i driver vesa; 
* ''Rescue a Fedora system'' (per chi avesse problemi di avvio per la mancata istallazione, ad esempio, di Grub2 oppure per modificare i files di una installazione esistente);
* ''Run a memory test'' (per controllare la memoria del sistema);
* ''Boot from local drive'';
* ''Return to main menù'' (per ritornare al menù principale).

In tutte le azioni è possibile premere il tasto tab per inserire eventuali opzioni, e sono tutte selezionabili, oltre che con le frecce, anche con la lettera di colore diverso rispetto al resto del testo.<br />
Con il tasto "esc", invece, si torna al riquadro precedente.<br />
Selezionando ''Install or upgrade Fedora'' e premendo invio, dopo la richiesta di verifica del supporto utilizzato, inizia l'installazione.<br />

{{suggerimento|E' sempre utile eseguire il controllo del supporto di installazione, come richiesto dall'installer, perchè se il dvd fosse corrotto, la sistemazione diventerebbe spesso impossibile, lasciando spazio solamente ad una nuova procedura ex-novo.}}

Anaconda provvede a chiedere la lingua di istallazione e, scegliendo, per esempio, l'italiano, sia la tastiera che l'orario verranno impostati già correttamente nelle successive selezioni, salvo si voglia differentemente.<br />
[[File:Install_f16_3.png|thumb|250px|left|E' necessario definire la destinazione d'uso dei dischi]]
Come da qualche versione a questa parte, l'installer richiede di qualificare il tipo di utilizzo del disco (o dei dischi) della nostra macchina; le opzioni che vengono proposte sono due:
* ''dispositivi di storage comuni'';
* ''dispositivi di storage speciale''.
Senza addentrarsi molto sulla differenziazione, la discriminante è data dal fatto che la seconda deve essere sicuramente utilizzata qualora, in una azienda, fosse presente un amministratore di sistema addetto alla gestione di vari volumi di tipo differente interconnessi fra loro, e non è il caso di utenti desktop.<br />[[File:Install_f16_5.png|thumb|250px|right|configurare la rete]]
Per cui l'opzione selezionata di default, con tutta probabilità, è quella che più risponde alle caratteristiche di un sistema desktop ad uso personale.<br />
Dopo il controllo dei dispositivi, Anaconda richiede di impostare l'hostname, e di configurare la rete (anche wifi) e premendo ''Configure network'' è possibile aggiungere la propria connessione, di qualunque tipo essa sia, utilizzando la modalità integrata di Network Manager.<br />
E' possibile, quindi, già in fase di prima installazione, installare i pacchetti più aggiornati prelevati direttamente dai repositori Fedora e, come spiegato successivamente, aggiuntivi.<br />

La schermata successiva ci chiede di calibrare il fuso orario preimpostato (avendo scelto la lingua italiana) su Roma e con l'UTC selezionato (per conoscere di più riguardo il tempo coordinato universale, su [http://it.wikipedia.org/wiki/Tempo_coordinato_universale Wikipedia] è possibile trovare una spiegazione esaustiva).<br />

{{Warning|Qualora sulla macchina fosse presente il sistema Windows, è necessario disattivare questa casella (UTC), in quanto l'orario verrebbe sfasato ad ogni avvio di detto s.o.}}
Successivamente occorre definire la password per root, che, come si sa, '''è quella più importante del sistema'''.

{{suggerimento|Sarebbe bene seguire le indicazioni di controllo del sistema (non ostativo) sulla forza della password di root scelta. Più è forte la password, più sicurezza si ha.}}

Proseguendo con l'installazione occorre prestare particolare attenzione alla parte successiva che è quella riguardante il partizionamento del disco.<br />

{{Warning|Nel caso fossero presenti 2 dischi fissi, è possibile che si crei confusione nella schermata di selezione di quelli da destinare all'installazione:
*'''Nella sezione a sinistra''', occorre inserire i dischi in cui sono '''presenti dati''' solamente da montare all'avvio (partizioni fat o ntfs o altro di interscambio); 
*'''Nella sezione a destra''' andranno inseriti i dischi che verranno '''utilizzati per l'installazione di fedora''' e di cui uno sarà contrassegnato come disco contenente il bootloader.}}

[[File:Install_f16_7.png|502px|left|presenza di più dischi fissi]][[File:Install_f16_6.png|502px|right|selezione in presenza di più dischi fissi]]<br  />
I dischi selezionati per l'installazione, saranno quelli interessati al contenimento del filesystem di Fedora, non necessariamente da formattare perchè solo successivamente verrà richiesto di specificare il tipo di partizionamento e la formattazione.

{{nota|Presenza contemporanea di Windows|Anche se nel disco che rimane a destra ci sono dei dati e lo si vuole considerare per utilizzarne nel LVM lo spazio rimasto libero, la parte già occupata non verrà  formattata, a meno di indicazioni differenti ad opera dell'utente. Per cui i propri dati sono comunque preservati..}} 

Si arriva, dunque, alla parte relativa al partizionamento che presenta diverse opzioni ed alla quale '''''bisogna prestare particolare attenzione'''<br />
Le scelte possono essere diverse, in base alla tipologia di installazione;
* Usare tutto lo spazio (verrà rimosso qualsiasi altro sistema operativo o dati presenti nel sistema);
* Sostituire il sistema linux esistenze (verrà rimosso qualsiasi sistema operativo Linux presente, non altri);
* Ridurre il sistema attuale (potranno essere ridimensionate le partizioni per ricavare nuovo spazio);
* Usare lo spazio libero (non verrà eliminato nè ridotto nulla, verrà solamente utilizzato l'eventuale spazio libero presente nel sistema);
* Creare una struttura personalizzata (è la modalità manuale dove, rispetto a versioni precedenti, occorre prestare attenzione alla partizione '''''bios''''' da creare qualora non si avesse un sistema EFI).

La stessa schermata prevede la selezione di default per l'uso di LVM, l'eventuale crittazione del sistema e l'eventuale revisione e modifica delle partizioni, consigliabile specialmente per quelle automatiche.<br />

In questa guida verrà utilizzata la prima selezione, ''usare tutto lo spazio'' (che imposta in automatico LVM) e, come detto, di rivederle utilizzando un unico disco fisso; chiaramente avendone a disposizione più di uno, si potrà distribuire il filesystem su tutti.
[[File:Install_f16_8.png|thumb|250px|left|creazione automatica di partizioni con LVM]]

'''Nota bene:''' la diversa nomenclatura e l'esigua capacità dei dischi sono state già spiegate in testa al presente documento.<br />

Da notare la necessaria partizione ''BIOS boot'', pertanto, che occorre ricordarsi nel caso di partizionamento manuale.<br />
E' possibile, come sempre, modificare le partizioni selezionando il tasto modifica.<br />

Tra le novità di questa release, come visto, c'è anche quella di '''grub2''', ed una delle caratteristiche di questo bootloader è quella di interpretare anche il filesystem '''brtfs''' sulla partizione di boot (in caso di partizionamento manuale occorre ricordarsi che deve essere di almeno 500 megabyte).<br />

Nel caso qui riportato ''brtfs'' verrà autilizzato sia su '''''/boot'''''  che su '''''/''''' che su '''''/home'''''; con quest'ultima da creare, se la si volesse utilizzare separatamente, riducendo la partizione di ''lv_root'' e ricavando quindi lo spazio da riservare a ''lv_home'').<br /> <br />


[[File:Install_f16_9.png|thumb|600px|center|riduzione partizione ''/'']]
[[File:Install_f16_10.png|thumb|600px|center|selezione spazio libero ricavato]]
[[File:Install_f16_11.png|thumb|600px|center|creazione partizione ''/home'' da spazio libero]]<br />

Il punto di montaggio della ''/home'', a cui è stato assegnato il nome ''"lv_home"'' (il nome potrebbe essere qualsiasi si possa immaginare, nell'esempio è stato usato questo per coerenza con lv_root) , quindi, sarà un volume logico all'interno del volume vg_mailga, il cui file system abbiamo detto essere ''brtfs''. Le dimensioni sono pari allo spazio che è stato tolto a lv_root.<br />

Nell'esempio in esame, per impossibilità di elencazione delle possibili alternative, è stato eseguito un partizionamento in cui si è cercato di spiegare sia l'automatismo che la modifica del risultato, comprendendo di fatto come poter fare nei casi automatico e manuale.<br />

Premendo avanti, ecco il fatidico avviso che segna l'inizio dell'avventura, la richiesta di autorizzazione alla creazione ed alla formattazione del filesystem.<br />
{{Warning|Dal momento in cui si acconsente a questa operazione, non c'è più possibilità di tornare allo stato originario del nostro disco.}}
Al termine della formattazione viene chiesto dove installare il bootloader (grub2).<br />

[[File:Install_f16_13.png|thumb|250px|right|opzioni installazione grub2]]

Automaticamente Anaconda propone l'MBR del disco dev/sda ma, premendo ''"cambia dispositivo"'' è possibile modificare le opzioni e anzichè installarlo sul MBR, si può inserirlo nel primo settore della partizione di boot che, nel caso in esempio è /dev/sda2. Ma se si fosse scelto di installare su un secondo disco Fedora perchè nel disco di avvio, ad esempio, è presente Windows, è possibile decidere di installare grub2 sul primo settore della partizione di /boot (cioè il disco sdb) e poi invertire l'ordine di avvio dei dischi com l'opzione ''BIOS drive order''.<br />

E' possibile decidere di inserire una password a livello di bootloader, cosa che aumenterebbe la sicurezza.<br />

Veniamo, infine, alla richiesta dell'installer circa il tipo di utilizzo del computer e l'eventuale inserimento di repositori aggiuntivi (come gli [http://rpmfusion.org rpmfusion] free e non free), i link ai quali sono da riportare all'interno della stessa schermata premendo ''Add additonal software repository'' e scrivendo all'interno della richiesta che appare:
* per gli rpmfusion-free l'indirizzo <nowiki>http://download1.rpmfusion.org/free/fedora/releases/16/Everything/i386/os</nowiki>
* per gli rpmfusion-nonfree l'indirizzo <nowiki>http://download1.rpmfusion.org/nonfree/fedora/releases/16/Everything/i386/os/</nowiki>
I repository ''Fedora'' ed ''Updates'' sono invece già presenti dando così la possibilità, attivandoli, di avere una istallazione fresca e già aggiornata.<br />
[[File:Install_f16_14.png|thumb|600px|center|inserimento repositori aggiuntivi]]
E' possibile, inoltre, attivare la richiesta  di modifica dei pacchetti selezionandone in aggiunta o deselezionandone altri scegliendo sia le categorie che pacchetti opzionali.<br />

Terminata la fase di installazione, viene chiesto il riavvio (al boot si potrà vedere grub2) che permette di inserire le informazioni di carattere generale e di impostare l'utenza desktop (il superuser era già stato impostato nella fase precedente).<br />
E' importante considerare di spedire il profilo, molto utile per il debug e ricordarsi di usare una password decisamente robusta anche per l'utente. Occorre anche specificare se si vuole utilizzare il proprio account user per compiti amministrativi, in alternativa a root.<br />

That's all folks, enjoy.

==Installazione con netinstall==

{{Warning|Con questa tipologia di installazione '''''è necessario''''' che il proprio personal computer sia connesso alla rete.}}

Anche in questo caso è necessario l'avvio da supporto magnetico.<br />

Procedendo con l'installazione non viene richiesto il controllo dell'integrità del disco, a differenza della versione DVD, mentre il menù per l'installazione è il medesimo per entrambe e presenta le stesse voci sia nel menù principale che in quello secondario.<br />

Ovviamente questa guida tratterà una installazione "fresh" del sistema operativo, perciò è necessario selezionare la prima voce.Fino ad ora le due installazioni (con netinstall e con DVD) possono essere parificate, tuttavia, nel prosieguo, la scelta di lingua e tastiera viene richiesta in formato testuale [[File:Net_install_f16_1.png|thumb|250px|right|richiesta lingua in formato testuale]]per poi tornare in modalità grafica per la scelta del dispositivo di storage, la configuarzione dell'host, e la verifica della connessione ('''''assolutamente necessaria perchè nel disco di installazione via rete non ci sono pacchetti residenti''''').
[[File:Install_f16_5.png|thumb|250px|left|configurazione della rete]]<br />

E' possibile configuare l'avvio automatico della scheda anche da qui, selezionando la casella Connettere automaticamente, utilizzando il NetworkManager integrato in Anaconda.<br />

Come in tutte le istallazioni in dual boot con sistemi Windows, anche in questo caso è necessario deselezionare la casella "L'orologio di sistema usa UTC" per evitare sfasature di orario se dovesse esserci la necessità di un cambio di sistema operativo.<br />

Viene richiesta, quindi, la password di root, per la quale verrà verificata la relativa robustezza.<br />

Proseguendo con l'installazione occorre prestare particolare attenzione alla parte successiva che è quella riguardante il partizionamento del disco.<br />

{{Warning|Nel caso fossero presenti 2 dischi fissi, è possibile che si crei confusione nella schermata di selezione di quelli da destinare all'installazione:
*'''Nella sezione a sinistra''', occorre inserire i dischi in cui sono '''presenti dati''' solamente da montare all'avvio (partizioni fat o ntfs o altro di interscambio); 
*'''Nella sezione a destra''' andranno inseriti i dischi che verranno '''utilizzati per l'installazione di fedora''' e di cui uno sarà contrassegnato come disco contenente il bootloader.}}
{{nota|Presenza contemporanea di Windows|Anche se nel disco che rimane a destra ci sono dei dati e lo si vuole considerare per utilizzarne nel LVM lo spazio rimasto libero, la parte già occupata non verrà  formattata, a meno di indicazioni differenti ad opera dell'utente. Per cui i propri dati sono comunque preservati..}} 

Si arriva, dunque, alla parte relativa al partizionamento che presenta diverse opzioni ed alla quale '''''bisogna prestare particolare attenzione'''<br />
Le scelte possono essere diverse, in base alla tipologia di installazione;
* Usare tutto lo spazio (verrà rimosso qualsiasi altro sistema operativo o dati presenti nel sistema);
* Sostituire il sistema linux esistenze (verrà rimosso qualsiasi sistema operativo Linux presente, non altri);
* Ridurre il sistema attuale (potranno essere ridimensionate le partizioni per ricavare nuovo spazio);
* Usare lo spazio libero (non verrà eliminato nè ridotto nulla, verrà solamente utilizzato l'eventuale spazio libero presente nel sistema);
* Creare una struttura personalizzata (è la modalità manuale dove, rispetto a versioni precedenti, occorre prestare attenzione alla partizione '''''bios''''' da creare qualora non si avesse un sistema EFI).

La stessa schermata prevede la selezione di default per l'uso di LVM, l'eventuale crittazione del sistema e l'eventuale revisione e modifica delle partizioni, consigliabile specialmente per quelle automatiche.<br />

In questa guida verrà utilizzata la prima selezione, ''usare tutto lo spazio'' (che imposta in automatico LVM) e, come detto, di rivederle utilizzando un unico disco fisso; chiaramente avendone a disposizione più di uno, si potrà distribuire il filesystem su tutti.

'''Nota bene:''' la diversa nomenclatura e l'esigua capacità dei dischi sono state già spiegate in testa al presente documento.<br />

Da notare la necessaria partizione ''BIOS boot'', che occorre ricordarsi nel caso di partizionamento manuale.<br />
E' possibile, come sempre, modificare le partizioni selezionando il tasto modifica.<br />

Tra le novità di questa release, come visto, c'è anche quella di '''grub2''', ed una delle caratteristiche di questo bootloader è quella di interpretare anche il filesystem '''brtfs''' sulla partizione di boot (in caso di partizionamento manuale occorre ricordarsi che deve essere di almeno 500 megabyte).<br />

Nel caso qui riportato ''brtfs'' verrà autilizzato sia su '''''/boot'''''  che su '''''/''''' che su '''''/home'''''; con quest'ultima da creare, se la si volesse utilizzare separatamente, riducendo la partizione di ''lv_root'' e ricavando quindi lo spazio da riservare a ''lv_home'').<br /> <br />

[[File:Install_f16_11.png|thumb|600px|center|creazione partizione ''/home'' da spazio libero]]<br />

Il punto di montaggio della ''/home'', a cui è stato assegnato il nome ''"lv_home"'' (il nome potrebbe essere qualsiasi si possa immaginare, nell'esempio è stato usato questo per coerenza con lv_root) , quindi, sarà un volume logico all'interno del volume vg_mailga, il cui file system abbiamo detto essere ''brtfs''. Le dimensioni sono pari allo spazio che è stato tolto a lv_root.<br />

Nell'esempio in esame, per impossibilità di elencazione delle possibili alternative, è stato eseguito un partizionamento in cui si è cercato di spiegare sia l'automatismo che la modifica del risultato, comprendendo di fatto come poter fare nei casi automatico e manuale.<br />

Premendo avanti, ecco il fatidico avviso che segna l'inizio dell'avventura, la richiesta di autorizzazione alla creazione ed alla formattazione del filesystem.<br />
{{Warning|Dal momento in cui si acconsente a questa operazione, non c'è più possibilità di tornare allo stato originario del nostro disco.}}
Al termine della formattazione viene chiesto dove installare il bootloader (grub2).<br />

Automaticamente Anaconda propone l'MBR del disco dev/sda ma, premendo ''"cambia dispositivo"'' è possibile modificare le opzioni e anzichè installarlo sul MBR, si può inserirlo nel primo settore della partizione di boot che, nel caso in esempio è /dev/sda2. Ma se si fosse scelto di installare su un secondo disco Fedora perchè nel disco di avvio, ad esempio, è presente Windows, è possibile decidere di installare grub2 sul primo settore della partizione di /boot (cioè il disco sdb) e poi invertire l'ordine di avvio dei dischi com l'opzione ''BIOS drive order''.<br />

E' possibile decidere di inserire una password a livello di bootloader, cosa che aumenterebbe la sicurezza.<br />

Veniamo, infine, alla richiesta dell'installer circa il tipo di utilizzo del computer e l'eventuale inserimento di repositori aggiuntivi (come gli [http://rpmfusion.org rpmfusion] free e non free), i link ai quali sono da riportare all'interno della stessa schermata premendo ''Add additonal software repository'' e scrivendo all'interno della richiesta che appare:
* per gli rpmfusion-free l'indirizzo <nowiki>http://download1.rpmfusion.org/free/fedora/releases/16/Everything/i386/os</nowiki>
* per gli rpmfusion-nonfree l'indirizzo <nowiki>http://download1.rpmfusion.org/nonfree/fedora/releases/16/Everything/i386/os/</nowiki>
I repository ''Fedora'' ed ''Updates'' sono invece già presenti dando così la possibilità, attivandoli, di avere una istallazione fresca e già aggiornata.<br />
[[File:Install_f16_14.png|thumb|600px|center|inserimento repositori aggiuntivi]]
E' possibile, inoltre, attivare la richiesta  di modifica dei pacchetti selezionandone in aggiunta o deselezionandone altri scegliendo sia le categorie che pacchetti opzionali.<br />

Proseguendo, l'installer si connetterà alla rete e preleverà i pacchetti direttamente dai repositori per l'installaizone dei pacchetti più aggiornati in quel momento. '''Nessun pacchetto verrà prelevato dal cd'''.<br />

Terminata la fase di installazione, viene chiesto il riavvio (al boot si potrà vedere grub2) che permette di inserire le informazioni di carattere generale e di impostare l'utenza desktop (il superuser era già stato impostato nella fase precedente).<br />
E' importante considerare di spedire il profilo, molto utile per il debug e ricordarsi di usare una password decisamente robusta anche per l'utente. Occorre anche specificare se si vuole utilizzare il proprio account user per compiti amministrativi, in alternativa a root.<br />

==Installazione da disco live==
